apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-travel-agent
  labels:
    app: ai-travel-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-travel-agent
  template:
    metadata:
      labels:
        app: ai-travel-agent
    spec:
      containers:
        - name: ai-travel-agent
          image: ai-travel-agent:latest
          ports:
            - containerPort: 8501
          envFrom:
            - secretRef:
                name: ai-travel-agent-secrets